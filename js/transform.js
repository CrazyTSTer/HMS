/**
 * Created by CrazyTSTer on 17.10.14.
 */
function transform_sagnificant_obj()
{
    var width = screen.width;
    var height = screen.height;

    if ((height<=800) && (width<=1280)) {
        /*#todo scale sagnificant objects */
    }
}